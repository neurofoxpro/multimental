name: Synchronize task graph and labels
on:
  push:
    branches: [dev]
permissions:
  contents: read
  issues: write
concurrency:
  group: multimental-issue-index
  cancel-in-progress: false
jobs:
  index:
    if: github.repository == 'neurofoxpro/multimental'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version-file: .nvmrc
      - name: Reconcile managed labels and native dependencies
        env:
          GH_TOKEN: ${{ github.token }}
        run: npm run game -- issue-graph sync
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: issue-graph-evidence
          path: .gameprod/evidence/issue-graph.json
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30
