# Multimental — продолжение текущей итерации

Дата: 25.09.2026. Основная память — GitHub Issue #29, задачи #30–86. Сначала прочитать живые Issues и состояние PR, а не считать этот файл текущим результатом.

## Текущий инженерный набор
Ветка `feature/github-memory-20260925`, PR #87. Исходная проверенная точка dev: `bd47932c15e84f1c17fec426b82a91eb07b9e518` (PR28, опубликованный .120.1). Head нового PR и проверки читать заново.

Добавлены живые команды памяти, перенос проверенных memory-модулей из лаборатории, параллельные Linux/Windows проверки, чистая модель профиля и 233 проверки модели в Godot. Дисковое сохранение, миграции, UI коллекции и META-05 целиком НЕ завершены.

`tools/ci-identity.mjs` и `game/src/profile_store.gd` не записаны: соответствующие операции остановлены инструментом в этой итерации. Через другой путь эти операции не повторять. Автоинтеграционные маршруты flow явно выключены до развернутого и проверенного source-seal adapter. Обычное разрешённое dev-вливание после автоматических проверок не означает, что автономный сервис уже включён.

## Команды
`npm run game -- resume` — 57 задач из живых Issues.
`npm run game -- task META-05` — поручение и обсуждение.
`npm run game -- github offline` — датированный локальный снимок, не свежая память.
`npm run game -- check` — существующий аудит и подготовка.
`scripts/chat.cmd publish "message" "title"` — стандартная публикация проверенного изменения и создание PR.
`node tools/source-tests.mjs` — независимые Node-тесты с ограниченным параллелизмом.

При редактировании нового/dirty файла использовать `apply` с точным `expectedCurrentSha256`, иначе он защищён от перезаписи. Не делать лишний коммит только ради возможности продолжить редактирование.

Ранее заявленный блок dispatch из-за отсутствия workflow в main опровергнут для зарегистрированного build.yml: настоящий запуск 36180893893 из dev прошёл. Main менять ради этого не требуется. Это не доказательство доступности любого нового workflow.

Станция только VENEL-SENDRIK. Телефон — отдельная стадия, не блок разработки; повторно проверить его доступность. Не удалять данные и не менять подпись. После завершения текущего инфраструктурного PR продолжать #30 → #31; исходную особую механику и смысл рейтинга не придумывать за владельца.
