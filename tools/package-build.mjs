import fs from 'node:fs';import path from 'node:path';import {sha,writeJSON,readJSON} from '../skills/game-production/scripts/lib.mjs';
const e=process.env;if(e.GITHUB_REPOSITORY!=='neurofoxpro/multimental')throw Error('Wrong repository');const apk='multimental-'+e.BUILD_VERSION+'-'+e.GITHUB_SHA.slice(0,7)+'.apk';if(!/^multimental-[\w.-]+\.apk$/.test(apk))throw Error('Invalid filename');fs.mkdirSync('artifacts',{recursive:true});fs.copyFileSync('dist/multimental-debug.apk',path.join('artifacts',apk));const digest=sha(fs.readFileSync(path.join('artifacts',apk)));const evidence=readJSON('.gameprod/evidence/build-android.json');if(evidence.status!=='passed')throw Error('Missing build receipt');writeJSON('artifacts/build-manifest.json',{schemaVersion:1,repository:e.GITHUB_REPOSITORY,commit:e.GITHUB_SHA,sourceBranch:e.GITHUB_REF_NAME,workflowRun:e.GITHUB_RUN_ID,version:e.BUILD_VERSION,versionCode:Number(e.BUILD_CODE),package:'pro.neurofox.multimental.dev',apk,sha256:digest,signing:'ephemeral CI debug; authorized device station applies persistent private local dev signature',gameRules:'prototype-v1',sourceDigest:evidence.sourceDigest});fs.writeFileSync('artifacts/SHA256SUMS.txt',digest+'  '+apk+'\n');
